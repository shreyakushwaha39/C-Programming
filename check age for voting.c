//Check a program to check whether a person is eligible for vote or not using if condition
#include<stdio.h>
int main()
{
	int age;
	printf("Enter your age:");
	scanf("%d",&age);
	if (age>=18)
	{
		printf("Congratulations!You are eligible for vote.");
	}
	else{
	}
	return 0;
}
